"""Family Link Alerts application package.

`__version__` is the single source of truth for the app's displayed version
(Settings page, etc.) -- bump it as part of cutting a release, in the same
commit as the matching `CHANGELOG.md` entry and just before tagging
`vX.Y.Z` (see README "Updating" for the full release runbook), so the
version shown in the UI always matches the git tag / GHCR image tag.
"""
__version__ = "1.11.0"
